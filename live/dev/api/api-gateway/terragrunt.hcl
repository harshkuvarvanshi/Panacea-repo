# ==========================================================
# INCLUDE ROOT CONFIG (Backend + Provider)
# ==========================================================
include {
  path = "${get_repo_root()}/live/root.hcl"
}
# ==========================================================
# MODULE SOURCE
# ==========================================================
terraform {
  source = "${get_repo_root()}/modules/api/api-gateway"
}

# ==========================================================
# INPUTS
# ==========================================================
inputs = {
  name = "panacea-http-api"
}